export interface Book {
    title: string,
    firstPublishYear: number,
    authorName: string
}
export interface BookResponse {
    title: string,
    first_publish_year: number,
    author_name: string
}